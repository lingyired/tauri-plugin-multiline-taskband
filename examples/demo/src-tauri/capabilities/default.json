{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the settings popup",
  "windows": ["main", "popup"],
  "permissions": [
    "core:default",
    "opener:default",
    "multiline-taskband:default"
  ]
}
