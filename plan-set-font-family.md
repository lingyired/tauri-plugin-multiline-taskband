# Plan: taskband 增加 `set_font_family`（与 menubar 对齐）

> 目标：给 `tauri-plugin-multiline-taskband` 增加 `set_font_family` 命令，使两个同家族插件的字体能力 API 完全对齐。
> 状态：**未实施**（仅规划）。

## 一、API 对称性约定

完全照搬 menubar 现有 `set_font_family` 的语义，保证同一份前端代码跨端复用：

- 命令名 `set_font_family`，参数 `id` + `top: Option<String>` + `bottom: Option<String>`
- `null` / `""` / 缺省 = 该行使用**系统默认字体**（**重置**语义，与 menubar 一致）
- 上下行独立；只改一行时另一行传 `null` 不动
- 字体名不存在时 GDI 静默回退系统字体（与 menubar「unknown names silently fall back」一致）

### 可行性确认（已查证）
taskband 文本渲染走 `CreateFontIndirectW` + `LOGFONTW`（`src/native/windows.rs:1275`）。
当前 `lfFaceName: [0u16; 32]` 全零 = 系统默认字体。只要把字体族名以 UTF-16 填入即可支持，
**无技术障碍**，且语义与 menubar 的 `NSFont` 路径能对上。

### 签名（最终形态）
```rust
fn set_font_family(&self, id: String, top: Option<String>, bottom: Option<String>) -> crate::Result<()>;
```
```ts
export interface SetFontFamilyOptions {
  id: string
  top: string | null
  bottom: string | null
}
export async function setFontFamily(options: SetFontFamilyOptions): Promise<void>
```

---

## 二、7 层改动清单（与加 `set_padding` 同构）

### 1. `src/models.rs` — 加请求结构
放在 `FontSizesRequest`（line 62）之后：
```rust
#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SetFontFamilyRequest {
    pub id: String,
    #[serde(default)]
    pub top: Option<String>,
    #[serde(default)]
    pub bottom: Option<String>,
}
```

### 2. `src/commands.rs` — 加命令
参考 `set_font_sizes`（line 36）写法：
```rust
#[command]
pub(crate) async fn set_font_family<R: Runtime>(
    app: AppHandle<R>,
    payload: SetFontFamilyRequest,
) -> crate::Result<()> {
    app.multiline_taskband().set_font_family(payload.id, payload.top, payload.bottom)
}
```

### 3. `src/lib.rs` — 注册 + trait 方法
- `generate_handler!` 宏（line 34）加 `commands::set_font_family`
- trait `MultilineTaskbandExt`（line 21）加：
  ```rust
  fn set_font_family(&self, id: String, top: Option<String>, bottom: Option<String>) -> crate::Result<()>;
  ```

### 4. `src/desktop.rs` — trait 方法实现
照 `set_font_sizes`（line 65）的 `#[cfg(target_os = "windows")]` / `#[cfg(not(...))]` 双分支结构，转发到 `native::set_font_family`。

### 5. `src/native/windows.rs`（核心，本机可编译验证）
- `Inst` 结构（line 191）加字段，默认 `None`：
  ```rust
  top_face: Option<String>,
  bottom_face: Option<String>,
  ```
  （`Default` 实现 line 220 同步补 `None`）
- `UiCommand` 枚举（line 91）加变体：
  ```rust
  SetFontFamily { id: String, top: Option<String>, bottom: Option<String> },
  ```
- 公开函数（照 `set_font_sizes` line 272）：
  ```rust
  pub fn set_font_family(id: String, top: Option<String>, bottom: Option<String>) -> crate::Result<()> {
      post(UiCommand::SetFontFamily { id, top, bottom });
      Ok(())
  }
  ```
- `handle_ui` 加匹配臂（照 `SetFontSizes` line 613）：更新 `inst.top_face`/`inst.bottom_face`，
  然后 `drop(guard); relayout_all();` —— **必须 relayout**，换字体改变文本宽度，邻居间距需重算
- `make_font`（line 1275）签名加 `face: Option<&str>`：
  - 非空 → `to_wide(face)` 截断到 31 字符填入 `lfFaceName`（保留末尾 NUL）
  - 空 / `None` → 维持 `[0u16; 32]` 全零（系统默认）
- `measure_text`（line 1181）与 `render_line_alpha`（line 1212）各加 `face: Option<&str>` 参数，透传给 `make_font`
- `relayout_all`（line 1174-1175）`measure_text` 调用传 `inst.top_face.as_deref()` / `inst.bottom_face.as_deref()`
- `paint_inst`（line 1370-1371）`render_line_alpha` 调用同样传对应 `face`

### 6. 权限 — `permissions/default.toml`
加 `allow-set-font-family`（与 `set-font-sizes` 同属「setup 期样式」类，已在 default 集；
demo 通过 `multiline-taskband:default` 自动获得，无需额外 grant）。
`permissions/autogenerated/commands/set_font_family.toml` 由 `tauri build` 代码生成，
跑 demo 的 `cargo check` 时自动产出，**无需手改**。

### 7. `guest-js/index.ts` — 类型与导出
照搬 menubar `SetFontFamilyOptions` 与 `setFontFamily`（menubar `guest-js/index.ts:148` / `:411`）：
```ts
export interface SetFontFamilyOptions {
  id: string
  /** Font family for the top line, or `null`/`''` for the system font. */
  top: string | null
  /** Font family for the bottom line, or `null`/`''` for the system font. */
  bottom: string | null
}
export async function setFontFamily(options: SetFontFamilyOptions): Promise<void> {
  return await invoke('plugin:multiline-taskband|set_font_family', { payload: options })
}
```
然后本地副本 `C:\Users\lingsmbp\tmp-guest-js-build` 重新 `npm run build`，
把 `dist-js/` 拷回仓库（dist-js 被 gitignore，是本地产物）。

---

## 三、陷阱（实施时对照）

- **`lfFaceName` 上限 31 字符**：必须 `.take(31)` 截断并保留 NUL 终止，超长不能 panic
- **换字体必须 relayout**：否则实例窗口宽度不更新、邻居间距错位（用 `relayout_all()`，同 `SetFontSizes`）
- **`to_wide` 复用**：字体名编码走已有 `to_wide()` helper（line 1189），不自造
- **NUL 字节**（可选加固）：可在 `set_font_family` 入口对含 NUL 的名字按 `Error::InvalidArgument` 拒绝，
  与 menubar 防御风格一致

---

## 四、可选（popup 完全对齐 menubar）

menubar 在 popup 快照下发 `topFontFamily`/`bottomFontFamily`（`menubar/src/desktop.rs:617`）。
taskband 的 `emit_to(popup, "multiline-taskband://popup//open", ...)` 快照目前不含字体族——
若要让 popup 预填字体，可在 payload 加这两个字段。**纯对齐可选，默认不做以免扩大 scope**，待定。

---

## 五、验证（本机即可全量验证，无需 Mac）

- `cargo check`（taskband）——**关键优势**：这次改的是真正的 Windows 代码，本机 Windows 能编译验证
  （不像之前 menubar 的 macOS 路径被 `#[cfg]` 挡掉只能静态核对）
- demo `cargo check` —— 触发权限代码生成 + 确认 `default.toml` 加 `allow-set-font-family` 后
  `multiline-taskband:default` 不报错
- `dist-js` 重建并拷回
- 手动验证（Win11 任务栏）：对某个实例调 `setFontFamily({ id, top: "Consolas" })`，
  肉眼确认字体变化 + 邻居间距正确 + 传 `null` 能复位

---

## 六、范围说明

- 改动**只限 taskband 当前仓库**，不动 menubar（menubar 已有 `set_font_family`，是本次对齐参照）
- 不创建 commit / 不 push，完成后由用户决定切分与提交
