# CLAUDE.md — tauri-plugin-multiline-taskband

Tauri v2 插件：在 **Windows 10 / 11 任务栏的左右两侧**渲染多组「双行文字」。
（macOS 端已有等价方案 `tauri-plugin-multiline-menubar`，本插件是其在 Windows 上的对应物，命名与 API 与其对齐。）

**平台范围：仅支持 Windows 10 / 11。** 不提供 macOS / Linux / mobile 支持，源码中不含移动端脚手架（mobile.rs 已移除）；非 Windows 平台上插件注册但所有命令返回 `UnsupportedPlatform`（仅用于让脚手架在 macOS 上可 `cargo check`）。

**独立插件定位**：本仓库只做「插件本身 + 一个可运行的 demo 应用」（参照 `tauri-plugin-multiline-menubar` 的 `examples/demo`）。宿主集成（如 fund01 等真实业务）由使用方自行处理，不在本仓库范围内。

---

## 1. 技术方案（已定，参照 TrafficMonitor）

采用 TrafficMonitor 的 **Win11 叠加式（overlay）** 做法，而不是硬改任务栏窗口树：

- 每个实例是一个**独立的顶层 `WS_EX_LAYERED` 分层透明窗口**，`WS_POPUP` 无边框。
- 叠加 `WS_EX_TOOLWINDOW`（不进任务栏）+ `WS_EX_NOACTIVATE`（不抢焦点）+ `WS_EX_TOPMOST`（fallback 时保 z-order，嵌入成功后系统自动清除）。
- **可点击**：左键 emit `click` 事件 + 自动弹设置 popup；右键 emit `click`（button="right"）。overlay 区域会拦截点击，其余任务栏不受影响。
- **背景 alpha = 1 而非 0**：`UpdateLayeredWindow` 创建的分层窗口命中测试**按像素 alpha**：alpha=0 完全透明的像素鼠标穿透到下方任务栏（即使 SetParent 已嵌入）。文字区域 alpha 正常，但 PAD / 字形间隙 alpha=0 → 点击会穿透。**把背景 alpha 改成 1**（视觉上完全不可见，合成色与任务栏色差 < 0.5%），整个 label 矩形即可命中。这是这种 overlay 方案能"点击"的关键。
- 左实例锚定在开始按钮右侧；右实例锚定在通知区（托盘）左侧；同侧多实例按创建顺序横向排开。
- 通过 `FindWindowEx` 找 `Shell_TrayWnd`（主屏）/ `Shell_SecondaryTrayWnd`（副屏）、`TrayNotifyWnd`（通知区）、`Start`（开始按钮）定位。
- 文字用 **GDI 白底黑字 DIB + 逐像素重着色** 实现任意颜色 + 抗锯齿（white-on-black 时 R=G=B=coverage，红通道即 alpha）。
- 一个**专属 UI 线程**拥有所有窗口并跑消息泵；命令通过 `mpsc` 通道 + `PostThreadMessageW(WM_APP_COMMAND)` 跨线程投递（Win32 对象必须在单线程创建）。
- `SetWinEventHook(EVENT_OBJECT_LOCATIONCHANGE)` 监听任务栏移动/缩放，触发全部实例重排。

**为什么不用 TrafficMonitor 的 Win7–10「挤开任务列表」手法**：Win11 把 `MSTaskListWClass` 锁死，挪不动；叠加式在 Win10/Win11 通用且更稳。

---

## 2. 当前进度（Win11 26200 / ARM64 已编译运行验证通过）

| 模块 | 文件 | 状态 |
|---|---|---|
| 插件骨架 | `Cargo.toml` / `src/lib.rs` / `build.rs` | ✅ 完成 |
| 公共模型 | `src/models.rs`（Side / Rect / ColorStyle / 各 Request） | ✅ 完成 |
| 命令层 | `src/commands.rs`（11 个 command） | ✅ 完成 |
| 桌面路由 | `src/desktop.rs`（含 `transmute_copy` 取 Wry 句柄） | ✅ 完成（已运行验证，保留中） |
| 错误层 | `src/error.rs` | ✅ 完成 |
| **Windows 原生** | `src/native/windows.rs`（~990 行核心） | ✅ **已编译 + Win11 实机运行验证** |
| JS/TS 绑定 | `guest-js/index.ts` / 根目录 `package.json` / `tsconfig.json` / `rollup.config.js` | ✅ 已构建（dist-js/ 三件套） |
| 权限 | `permissions/default.toml` + `permissions/autogenerated/commands/*.toml` | ✅ 完成 |
| 文档 | `README.md`（验证清单已打勾） | ✅ 完成 |
| **Demo 应用** | `examples/demo/`（参照 multiline-menubar 的 demo） | ✅ **已建并在 Win11 运行验证** |

**命名已对齐 menubar**：插件名 `tauri-plugin-multiline-taskband`；Rust 结构体 `MultilineTaskband`、扩展 trait `MultilineTaskbandExt`、访问器 `app.multiline_taskband()`（对应 menubar 的 `MultilineMenubar` / `multiline_menubar()`）；JS 包名 `tauri-plugin-multiline-taskband-api`；事件前缀 `multiline-taskband://`；capability 权限 `multiline-taskband:default`。

**已验证（Windows 11 Pro build 26200 / ARM64，rustc 1.98 msvc）**：`cargo check` / `build` / `clippy` 全绿；`examples/demo` 实机运行，任务栏左右双行文字渲染、即时刷新、点击任务栏不消失（keep_on_top 定期置顶）、left 贴任务栏最左、点击穿透全部正常。

**编译修复历史要点**（从"从未编译"到可运行的关键改动）：
- **模块树缺失**：`src/native/windows.rs` 原本从未被任何 `mod` 声明引用 → 新增 `src/native/mod.rs` + lib.rs `#[cfg(windows)] mod native;` + desktop.rs `use crate::native::windows as native;`。
- Cargo.toml 补 3 个 windows-sys feature：`Win32_System_Threading` / `Win32_System_LibraryLoader` / `Win32_System_WindowsProgramming`。
- windows.rs：`wide_const` 改 const fn；`SetBkMode(TRANSPARENT as i32)`；`lfWeight as i32`；`CreateDIBSection` hsection 传 `0`；`app.emit(&format!(...))`；`COLOR_BTNTEXT`（18，勿用 15）；`GetMessageW` 返回 -1 时 break。
- **Win11 点击任务栏 overlay 消失**（build 26200）：任务栏被点击后 z-order 提升压过非激活分层窗口 → UI 线程 500ms 定时器 `keep_on_top()`：`GetWindow(GW_HWNDPREV)` 检测被任务栏压住才 `SetWindowPos(HWND_TOPMOST)`（先检测后置顶，避免消息风暴）。
- **left 定位**：Win11 居中布局 Start 在中间 → left 实例改为贴任务栏最左（`rect.left + 2`），不再找 `Start` 按钮。
- WinEvent hook 过滤扩大到任务栏子窗口（`IsChild`）+ 50ms 节流，点击任务栏/托盘动画也能触发重排。

---

## 3. Windows 验证清单（续做项：Win10 + 剩余手动项）

已完成（Win11 26200 / ARM64）：编译链全绿、任务栏渲染、多实例不重叠、即时刷新、点击任务栏不消失、left 贴最左、overlay 区域可点击（左键弹 popup / 右键发 click 事件）、暗/亮模式 default 色。

待完成：
1. **Win10 实机验证**（需 Win10 机器/VM）——主要回归渲染、定位、点击行为（Win10 上 `Shell_TrayWnd`/`TrayNotifyWnd`/`Start` 类名更稳定，但 Win11 的命中测试行为可能与 Win10 不同）。
2. **任务栏移动/缩放自动重排**（hook 已实现，手动验证：拖任务栏到顶部/切分辨率）。
3. **explorer 重启**（`taskkill /f /im explorer.exe && start explorer`）不留孤儿窗口。
4. **`desktop.rs` transmute_copy**：已运行验证无崩溃（决策：能跑就保留）。若后续接入非 Wry 宿主再改 `AppHandle<Wry>` 限定。
5. **DPI 100%/150%/200%** 多档验证（当前 `GetDpiForSystem`，若副屏/多 DPI 错位改 `GetDpiForWindow(taskbar.hwnd)`）。
6. **多显示器**：副屏任务栏（`Shell_SecondaryTrayWnd`）未处理，主屏够用即跳过。
7. **Popup 交互链路** ✅ **已在 Win11 26200 / ARM64（200% DPI）实机验证**：点击 overlay → popup 打开定位到 item 正上方（多实例各自锚定 + monitor clamp）、实例状态下发与预填（header、top/bottom 文字等）、点外部 → 失焦关闭。验证脚本：`C:\Users\lingsmbp\tmp-demo-cli\verify_popup.py`（构造截图存 `popup_top.png` / `popup_right3.png`）。

---

## 4. JS 绑定构建

```bash
cd guest-js
pnpm install
pnpm build        # rollup 产出 dist-js/，demo 前端 import 用
```
（macOS 上可做，但需在 Windows 验证 import 路径与权限声明是否齐全。）

---

## 5. Demo 应用（参照 multiline-menubar 的 `examples/demo`）

目标：在 `examples/demo/` 建一个**完整可运行的 Tauri v2 demo 应用**，用来在 Windows 上验证插件的全部能力。结构照抄 `tauri-plugin-multiline-menubar/examples/demo`：

1. **`examples/demo/src-tauri/Cargo.toml`**：
   ```toml
   [dependencies]
   tauri = { version = "2", features = [] }
   tauri-plugin-opener = "2"
   tauri-plugin-multiline-taskband = { path = "../../.." }
   serde = { version = "1", features = ["derive"] }
   serde_json = "1"
   ```
2. **`src-tauri/src/lib.rs` / `main.rs`**：注册插件
   ```rust
   tauri::Builder::default()
       .plugin(tauri_plugin_multiline_taskband::init())
       .run(tauri::generate_context!())
   ```
   （Rust 宿主侧通过 `app.multiline_taskband()` 扩展访问，与 `app.multiline_menubar()` 对齐。）
3. **`src-tauri/capabilities/default.json`**：加 `"multiline-taskband:default"`。
4. **前端用 vanilla HTML/JS**（不引框架，与 menubar demo 一致）：
   - 控制面板：创建/删除实例（可指定 `id`、`side`=left/right、初始 `top`/`bottom` 文字）；列出所有实例。
   - 每个实例提供全套调节：`setText` / `setFontSizes` / `setPadding`（左右边距，物理 px）/ `setColors`（`default` 或 `#rrggbb`，可做红涨绿跌示例）/ `setBold` / `setAlignment` / `setVisible` / `rect` / `isVisible`。
   - 用 `onReady(id)` 监听实例就绪后同步 UI 状态。
5. **运行**：`cd examples/demo && pnpm install && pnpm tauri dev`（需在 Windows 上跑）。
6. **demo 验收标准**：左右各建 2–3 个实例且不重叠；改文字/颜色/字号即时生效；任务栏移动/缩放自动重排；点击文字区域不挡任务栏点击。

> 宿主集成（如 fund01）不在本仓库范围。插件 API 稳定后，由使用方在各自 Tauri 工程里以 path 依赖接入（同 menubar 的接入方式），本仓库只负责保证插件本身可用且 demo 能完整演示。

---

## 6. API 速查（已落地命令）

`create(id, side)` · `remove(id)` · `set_text(id, top, bottom)` · `set_font_sizes(id, top, bottom)` · `set_padding(id, left, right)` · `set_side(id, side)` · `set_order(id, order)` · `set_margin(margin)` · `set_colors(id, top, bottom)` · `set_bold(id, top, bottom)` · `set_alignment(id, top, bottom)` · `set_visible(id, visible)` · `set_line_visible(id, top, bottom)` · `rect(id)` · `is_visible(id)` · `set_popup_window(label)` · `set_auto_popup(enabled)` · `open_popup(id)` · `close_popup(id)` · `toggle_popup(id)`

- `Side`：`"left"` / `"right"`（默认 right）。`set_side` 可把已存在实例动态挪到另一侧（不重建，保留 order）。
- `set_order`：同侧实例按 `order` 升序排开（默认=创建顺序）；交换相邻 order 值即可实现上移/下移。
- `set_margin`：全局实例间距（物理 px，默认 4）；行内两行文字间距是固定内部样式（`LINE_GAP`），不受影响。
- `set_line_visible`：单行显示/隐藏（top/bottom 各自独立，默认全显示）。隐藏一行 → 实例窗口收缩为另一行、由 `center_y` 自动在任务栏**垂直居中**；两行都隐藏 → 等同整实例隐藏（窗口 SW_HIDE、relayout 不占位），但实例级 `visible` 标志不变，重新显示任一行即恢复。`is_visible` 仍返回实例级标志；单行状态经 `popup-open` 快照的 `topVisible`/`bottomVisible` 下发。

- `Side`：`"left"` / `"right"`（默认 right）。
- `ColorStyle`：`{ "type": "default" }` 跟随系统明暗；`{ "type": "solid", "value": "#FF4F44" }` 固定色。
- 上下行视觉只由 `setFontSizes`（pt，默认 11/11）控制，无布局预设；`setPadding` 设每实例左右边距（物理 px，默认 4/4）。
- `alignment` 每行独立：`0`=左 / `1`=中 / `2`=右。
- Rust 宿主：`app.multiline_taskband()`（扩展 trait `MultilineTaskbandExt`）。
- 实例就绪后 native 层 `emit("multiline-taskband://{id}//ready", {id})`。
- 点击事件：native 层在 `WM_LBUTTONDOWN`/`WM_RBUTTONDOWN` 时 `emit("multiline-taskband://{id}//click", {id, position, rect, button, buttonState:"down"})`；左键且 `auto_popup` 开启时自动 `toggle_popup`。
- Popup 机制（对齐 menubar）：宿主在 `tauri.conf.json` 声明一个 `visible:false` 的 Tauri 窗口（label 默认 `"popup"`，用 `set_popup_window` 覆盖），插件在点击时 `get_webview_window` 定位（底部任务栏→item 上方居中，顶部→下方，纵向→外侧，含多显示器 clamp）、`show`、`set_focus`，并把该实例当前全部样式经 `emit_to(popup, "multiline-taskband://popup//open")` 下发；失焦自动 `hide`。`popup-open`/`popup-close` 按实例 id 发到 `multiline-taskband://{id}//popup-open|close`。

---

## 7. 与 TrafficMonitor 的差异（设计取舍）

| 维度 | TrafficMonitor | 本插件 |
|---|---|---|
| 窗口归属 | `SetParent` 挂成任务栏子窗口 | 独立顶层窗口，仅定位贴合 |
| 左/右手法 | Win10 挤开任务列表；Win11 叠加 | 统一叠加（两端通用） |
| 交互 | 可点击、可拖 | v2 可点击（左键弹 popup，右键发 click 事件）；背景 alpha=1 trick 保命中 |
| 渲染 | D2D / GDI 双后端 | 仅 GDI + 分层窗口 |
| 行可见性 | 无（文本留空仍占一整行） | `set_line_visible` 单行隐藏：单行时窗口收缩并垂直居中，双隐藏=整实例隐藏且不占位 |
| 多实例 | 单一 | N 实例，按 id/side 多分组 |
| 平台 | Windows 专用 | Tauri 插件，macOS 另由 multiline-menubar 承担 |

---

## 8. 已知限制（先对齐预期）

- 仅支持 Windows 10 / 11；无 macOS / Linux / mobile 支持。副屏任务栏未处理（见 §3.5）。
- overlay 窗口区域**可点击**（左键弹 popup / 右键发 `click` 事件），会拦截该小块区域对任务栏的点击；其余任务栏区域不受影响。
- 多实例同侧空间有限，需调用方控制数量。
- Popup 交互已实现但**仅经 cargo 编译验证**，尚未在 Win11 实机跑通（点击→弹窗→改样式→失焦关闭的完整链路），见 §3 验证清单。
